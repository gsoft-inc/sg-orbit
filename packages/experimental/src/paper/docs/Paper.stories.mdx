import { ArgsTable, Meta, Story } from "@storybook/addon-docs";
import { ComponentInfo, Tagline, Preview, ExperimentalMessage } from "@stories/components";
import { InnerPaper, Paper } from "@experimental/paper";

<Meta
    title="Experimental/Paper"
    id="paper"
/>

# Paper

<Tagline>A Paper is a general purpose component that can be used to compose new components that require a background with shadow. </Tagline>

<ComponentInfo
    usage={"import { Paper } from \"@orbit-ui/experimental-components\";"}
    githubPath="/packages/experimental/src/paper/src"
/>

## Usage

<ExperimentalMessage />

### Default

A default paper.

<Preview>
    <Story name="default">
        <Paper padding={4}>Paper is one of the most basic ui component.</Paper>
    </Story>
</Preview>

## API

<ComponentInfo
    usage={"import { Paper } from \"@orbit-ui/experimental-components\";"}
    inherits={[InnerPaper.defaultElement, "styled-component"]}
    compact
/>

<ArgsTable of={InnerPaper} sort="alpha" />
