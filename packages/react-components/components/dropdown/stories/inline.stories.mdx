import { Dropdown } from "@orbit-ui/react-dropdown/src";
import { Installation, ReactComponentsLayout } from "@blocks/react-components";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { RefLogger, createComponentSection, paramsBuilder } from "@utils";
import { WarningMessage } from "@blocks";
import Imports from "./imports.mdx";

<Meta
    title={createComponentSection("Dropdown/inline")}
    parameters={paramsBuilder()
        .docsLayout(<ReactComponentsLayout semanticPath="/modules/dropdown" />)
        .build()}
/>

# Inline Dropdown

An inlined dropdown can appear inline with other content.

## Installation

<Installation componentFolder="dropdown" imports={<Imports />} />

## Usage

<WarningMessage>
    This component have the same feature set as the <a href="/?path=/docs/components-dropdown-standard--default-story">standard dropdown</a> component.
</WarningMessage>

### Constants

The following constants are used in the examples below:

export const FRIENDS = [
    { key: "Jenny Hess", text: "Jenny Hess", value: "Jenny Hess" },
    { key: "Elliot Fu", text: "Elliot Fu", value: "Elliot Fu" }
];

```javascript dark
const FRIENDS = [
    { key: "Jenny Hess", text: "Jenny Hess", value: "Jenny Hess" },
    { key: "Elliot Fu", text: "Elliot Fu", value: "Elliot Fu" }
];
```

### Default

A default inlined dropdown.

<Preview>
    <Story name="default">
        <Dropdown
            placeholder="select friend"
            inline
            options={FRIENDS}
        />
    </Story>
</Preview>

### In a block

An inlined dropdown can be embedded in a block.

<Preview>
    <Story name="in a block">
        <div>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque vestibulum et<br /> et lacus at euismod <Dropdown
            placeholder="select friend"
            inline
            options={FRIENDS}
        /> elementum viverra maximus.</div>
    </Story>
</Preview>

### Disabled

An inlined dropdown can be disabled.

<Preview>
    <Story name="disabled">
        <Dropdown
            placeholder="select friend"
            inline
            disabled
            options={FRIENDS}
        />
    </Story>
</Preview>

<Story
    name="autofocus"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <div className="flex items-space-between">
        <Dropdown autofocus inline placeholder="select friend" options={FRIENDS} />
        <Dropdown autofocus inline disabled placeholder="select friend" options={FRIENDS} />
    </div>
</Story>

<Story
    name="autofocus delay"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <div className="flex items-space-between">
        <Dropdown autofocus inline autofocusDelay={500} placeholder="select friend" options={FRIENDS} />
        <Dropdown autofocus inline disabled autofocusDelay={500} placeholder="select friend" options={FRIENDS} />
    </div>
</Story>

<Story
    name="ref"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <RefLogger component={<Dropdown inline options={FRIENDS} />} />
</Story>
