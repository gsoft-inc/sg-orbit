import { ComponentImport } from "@blocks/react-components";

<ComponentImport
    bundle={`
        import { Toggle } from "@orbit-ui/react-components";
    `}
    standalone={`
        import { Toggle } from "@orbit-ui/react-input";
    `}
/>
