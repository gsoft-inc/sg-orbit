import { ComponentImport } from "@blocks/react-components";

<ComponentImport
    bundle={`
        import { Input } from "@orbit-ui/react-components";
    `}
    standalone={`
        import { Input } from "@orbit-ui/react-input";
    `}
/>
