import { ComponentImport } from "@blocks/react-components";

<ComponentImport
    bundle={`
        import { Checkbox } from "@orbit-ui/react-components";
    `}
    standalone={`
        import { Checkbox } from "@orbit-ui/react-input";
    `}
/>
