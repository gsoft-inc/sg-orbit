import { ComponentImport } from "@blocks/react-components";

<ComponentImport
    bundle={`
        import { Radio } from "@orbit-ui/react-components";
    `}
    standalone={`
        import { Radio } from "@orbit-ui/react-input";
    `}
/>
