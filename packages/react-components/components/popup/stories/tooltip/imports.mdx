import { ComponentImport } from "@blocks/react-components";

<ComponentImport
    bundle={`
        import { Tooltip } from "@orbit-ui/react-components";
    `}
    standalone={`
        import { Tooltip } from "@orbit-ui/react-popup";
    `}
/>
