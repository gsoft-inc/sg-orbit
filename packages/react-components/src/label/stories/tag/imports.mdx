import { ComponentImport } from "@blocks";

<ComponentImport
    bundle={`
        import { Tag } from "@orbit-ui/react-components";
    `}
    standalone={`
        import { Tag } from "@orbit-ui/react-label";
    `}
/>
