import { ComponentImport } from "@blocks";

<ComponentImport
    bundle={`
        import {
            MultiSelect,
            multiSelectItem
        } from "@orbit-ui/react-components";
    `}
    standalone={`
        import {
            MultiSelect,
            multiSelectItem
        } from "@orbit-ui/react-multi-select";
    `}
/>
