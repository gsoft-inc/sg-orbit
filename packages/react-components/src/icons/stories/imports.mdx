import { ComponentImport } from "@blocks";

<ComponentImport
    bundle={`
        import {
            Icon,
            MultiVariantIcon
        } from "@orbit-ui/react-components";
    `}
    standalone={`
        import {
            Icon,
            MultiVariantIcon
        } from "@orbit-ui/react-icons";
    `}
/>
