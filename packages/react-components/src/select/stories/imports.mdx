import { ComponentImport } from "@blocks";

<ComponentImport
    bundle={`
        import {
            Select,
            selectItem
        } from "@orbit-ui/react-components";
    `}
    standalone={`
        import {
            Select,
            selectItem
        } from "@orbit-ui/react-select"";
    `}
/>
