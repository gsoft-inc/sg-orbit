import { InstallReactComponents, ReactComponentsLayout } from "@blocks";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { Tab } from "@react-components/tab";
import { createComponentSection, paramsBuilder } from "@utils";
import Imports from "./imports.mdx";

<Meta
    title={createComponentSection("Tab")}
    parameters={paramsBuilder()
        .docsLayout(<ReactComponentsLayout semanticPath="/modules/tab" />)
        .build()}
/>

# Tab

A tab is a hidden section of content activated by a Menu.

## Installation

<InstallReactComponents imports={<Imports />} />

## Usage

### Default

A default tab.

<Preview>
    <Story name="default">
        <Tab
            menu={{ secondary: true, pointing: true }}
            panes={[
                {
                    menuItem: "Star",
                    render: () => <Tab.Pane attached={false}>Tab 1 Content</Tab.Pane>
                },
                {
                    menuItem: "Rocket",
                    render: () => <Tab.Pane attached={false}>Tab 2 Content</Tab.Pane>
                },
                {
                    menuItem: "Galaxy",
                    render: () => <Tab.Pane attached={false}>Tab 3 Content</Tab.Pane>
                }
            ]}
        />
    </Story>
</Preview>

### Labelled

A tab can include a label to draw attention.

TODO once Lozenged component exist

### Spacing

The tab content spacing can be customized.

<Preview>
    <Story name="spacing">
        <Tab
            menu={{ secondary: true, pointing: true }}
            panes={[
                {
                    menuItem: "Star",
                    render: () => (
                        <Tab.Pane attached={false}>
                            <div className="pa8">
                                <p>Tab 1 Content</p>
                            </div>
                        </Tab.Pane>
                    )
                },
                {
                    menuItem: "Rocket",
                    render: () => (
                        <Tab.Pane attached={false}>
                            <div className="pa8">
                                <p>Tab 2 Content</p>
                            </div>
                        </Tab.Pane>
                    )
                },
                {
                    menuItem: "Galaxy",
                    render: () => (
                        <Tab.Pane attached={false}>
                            <div className="pa8">
                                <p>Tab 3 Content</p>
                            </div>
                        </Tab.Pane>
                    )
                }
            ]}
        />
    </Story>
</Preview>
