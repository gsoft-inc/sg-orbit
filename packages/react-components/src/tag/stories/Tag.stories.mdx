import { Badge } from "@react-components/badge";
import { Button } from "@react-components/button";
import { CheckCircleIcon, CheckIcon, CrossIcon } from "@react-components/icons";
import { InnerTag, Tag } from "@react-components/tag";
import { InstallReactComponents, Props, ReactComponentsLayout } from "@blocks";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { RefLogger, createComponentSection, paramsBuilder } from "@utils";
import Imports from "./Imports.mdx";

<Meta
    title={createComponentSection("Tag")}
    parameters={paramsBuilder()
        .canvasLayout({ width: "80%" })
        .docsLayout(<ReactComponentsLayout sourcePath="/packages/react-components/src/tag/src" />)
        .build()}
/>

# Tag

A tag represent an interactive keyword that help label, organize and categorize content. Tags are meant to be displayed in a [TagsPicker](?path=/story/components-tagspicker--default-story).

## Installation

<InstallReactComponents imports={<Imports />} />

## Props

<Props
    componentsDefinitions={[
        { displayName: "Tag", component: InnerTag }
    ]}
/>

## Usage

### Default

A default tag.

<Preview>
    <Story name="default">
        <Tag>Falcon 9</Tag>
    </Story>
</Preview>

### Button

A tag can contain a [button](?path=/docs/components-button--default-story).

<Preview>
    <Story name="button">
        <Tag button={<Button icon={<CrossIcon />} />}>
            Falcon 9
        </Tag>
    </Story>
</Preview>

### Icon

A tag can contain a left or right [icon](?path=/docs/materials-icons--page).

<Preview>
    <Story name="icon">
        <div className="flex items-space-between">
            <Tag iconLeft={<CheckCircleIcon />}>
                Falcon 9
            </Tag>
            <Tag iconRight={<CheckIcon />}>
                Falcon 9
            </Tag>
        </div>
    </Story>
</Preview>

### Badge

A tag can contain a left or right [badge](?path=/docs/components-badge--default-story).

<Preview>
    <Story name="badge">
        <div className="flex items-space-between">
            <Tag badgeLeft={<Badge variant="dot" className="bg-primary-500" />}>
                Falcon 9
            </Tag>
            <Tag badgeRight={<Badge>6</Badge>}>
                Falcon 9
            </Tag>
        </div>
    </Story>
</Preview>

### Size

A tag can vary in size.

Can be set to: `"small"`, `"large"` or omitted.

<Preview>
    <Story name="size">
        <div className="flex items-end items-space-between">
            <Tag size="small">Falcon 9</Tag>
            <Tag>Falcon 9</Tag>
            <Tag size="large">Falcon 9</Tag>
            <Tag size="small" badgeLeft={<Badge variant="dot" className="bg-primary-500" />}>Falcon 9</Tag>
            <Tag badgeLeft={<Badge variant="dot" className="bg-primary-500" />}>Falcon 9</Tag>
            <Tag size="large" badgeLeft={<Badge variant="dot" className="bg-primary-500" />}>Falcon 9</Tag>
        </div>
    </Story>
</Preview>

### Outline

A tag can have a subtle appearance.

<Preview>
    <Story name="outline">
        <Tag variant="outline">Falcon 9</Tag>
    </Story>
</Preview>

### Custom colors

Custom colors can be applied to a tag.

<Preview>
    <Story name="custom-color">
        <Tag
            style={{
                backgroundColor: "#FFF6E7",
                color: "var(--marine-500)",
                border: "1px solid #FEE9C3"
            }}
        >
            Falcon 9
        </Tag>
    </Story>
</Preview>

<Story
    name="ref"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <RefLogger><Tag>Falcon 9</Tag></RefLogger>
</Story>



