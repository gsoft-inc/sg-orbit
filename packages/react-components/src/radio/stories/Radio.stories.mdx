import { Badge } from "../../badge";
import { ControlledRadioGroup } from "./ControlledRadioGroup.sample";
import { FilePreview, HocWarning, InstallReactComponents, Props, ReactComponentsLayout, WarningMessage } from "@blocks";
import { InfoIcon, LightbulbIcon } from "@react-components/icons";
import { InnerRadio, Radio } from "@react-components/radio";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { RefLogger, createComponentSection, paramsBuilder } from "@utils";
import Imports from "./Imports.mdx";

<Meta
    title={createComponentSection("Radio")}
    parameters={paramsBuilder()
        .docsLayout(<ReactComponentsLayout semanticPath="/modules/checkbox" />)
        .build()}
/>

# Radio

Radio buttons allow a user to select one option from a set.

## Installation

<InstallReactComponents imports={<Imports />} />

## Props

<HocWarning componentName="Checkbox" semanticPath="modules/checkbox/" />

<Props
    componentsDefinitions={[
        { displayName: "Radio", component: InnerRadio }
    ]}
/>

## Usage

<WarningMessage>
    This component have the same feature set as the <a href="?path=/docs/components-checkbox--text">checkbox</a> component.
</WarningMessage>

### Text

A radio can have a text.

<Preview>
    <Story name="text">
        <Radio text="Mars"/>
    </Story>
</Preview>

### Selected

A radio can be selected.

<Preview>
    <Story name="selected">
        <Radio defaultChecked text="Mars"/>
    </Story>
</Preview>

### Disabled

A radio can be disabled.

<Preview>
    <Story name="disabled">
        <Radio disabled text="Mars"/>
    </Story>
</Preview>

### Readonly

A radio can be readonly.

<Preview>
    <Story name="readonly">
        <Radio readOnly text="Mars" />
    </Story>
</Preview>

### No text

A radio doesn't require a text.

<Preview>
    <Story name="no text">
        <Radio />
    </Story>
</Preview>

### Icons

A radio can display a single or multiple [icons](?path=/docs/materials-icons--page) after his text.

<Preview>
    <Story name="icons">
        <div className="flex items-lot-of-space-between">
            <Radio text="Milky Way" icons={<LightbulbIcon />} />
            <Radio text="Milky Way" icons={[<LightbulbIcon />, <InfoIcon />]} />
        </div>
    </Story>
</Preview>

### Badge

A radio can have a [badge](?path=/docs/components-badge--default-story) after his text.

<Preview>
    <Story name="badge">
        <div className="flex items-lot-of-space-between">
            <Radio text="Milky Way" badge={<Badge>6</Badge>} />
            <Radio text="Milky Way" badge={<Badge inline>6</Badge>} />
        </div>
    </Story>
</Preview>

### Size

A radio can vary in size.

Can be set to: `"small"`, `"large"` or omitted.

<Preview>
    <Story name="size">
        <div className="flex items-end items-lot-of-space-between">
            <Radio size="small" text="Mars" />
            <Radio text="Mars" />
            <Radio size="large" text="Mars" />
        </div>
    </Story>
</Preview>

### Group

Radios buttons in a group must be controlled components.

<FilePreview filePath="/radio/stories/ControlledRadioGroup">
    <ControlledRadioGroup />
</FilePreview>

<Story
    name="ref"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <RefLogger component={<Radio text="Mars" />} />
</Story>
