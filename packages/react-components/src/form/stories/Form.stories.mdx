import { Button, ButtonGroup } from "@react-components/button";
import { Checkbox } from "@react-components/checkbox";
import { Field, HelpMessage, Label } from "@react-components/field";
import { FilePreview, InstallReactComponents, Props, ReactComponentsLayout } from "@blocks";
import { Form, InnerForm, InnerRow, Row } from "@react-components/form";
import { Inline } from "@react-components/layout";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { PasswordInput, TextInput } from "@react-components/input";
import { RefLogger, createComponentSection, paramsBuilder } from "@utils";
import { RegistrationForm } from "./RegistrationForm.sample";
import { TextLink } from "@react-components/link";
import Imports from "./Imports.mdx";

<Meta
    title={createComponentSection("Form")}
    parameters={paramsBuilder()
        .docsLayout(<ReactComponentsLayout sourcePath="/packages/react-components/src/form/src" />)
        .build()}
/>

# Form

Forms are commonly used to provide user interaction in web applications.

## Installation

<InstallReactComponents imports={<Imports />}/>

## Props

<Props
    componentsDefinitions={[
        { displayName: "Form", component: InnerForm },
        { displayName: "Row", component: InnerRow }
    ]}
/>

## Accessibility

When combined with [field](?path=/story/components-field--input) components, a form component follows the [WAI specifications for forms](https://www.w3.org/WAI/tutorials/forms/).

## Usage

In additional to wrapping a form element, a form component provide layout for fields and buttons. **To apply the layout correctly, every inputs must be wrapped in a field component.**

### Vertical layout

A form can stack fields vertically.

<Preview>
    <Story name="vertical layout">
        <Form fluid className="w-25">
            <Field required>
                <Label>Username</Label>
                <TextInput placeholder="john@spacex.com" />
            </Field>
            <Field required>
                <Label>Password</Label>
                <PasswordInput />
                <HelpMessage>I bet it's a strong one!</HelpMessage>
            </Field>
            <Field>
                <Checkbox>Remember me</Checkbox>
            </Field>
            <Button type="submit">Login</Button>
        </Form>
    </Story>
</Preview>

### Inline layout

Multiple form fields can be inlined on the same row.

<Preview>
    <Story name="inline layout">
        <Form fluid>
            <Row>
                <Field>
                    <Label required>First name</Label>
                    <TextInput />
                </Field>
                <Field>
                    <Label required>Last name</Label>
                    <TextInput />
                </Field>
                <Field>
                    <Label required>Username</Label>
                    <TextInput />
                </Field>
            </Row>
            <Row>
                <Field className="w-50">
                    <Label>City</Label>
                    <TextInput />
                </Field>
                <Field className="w-25">
                    <Label>State</Label>
                    <TextInput />
                </Field>
                <Field className="w-25">
                    <Label>Zip</Label>
                    <TextInput />
                </Field>
            </Row>
            <Field>
                <Checkbox>Agree to terms and conditions</Checkbox>
            </Field>
            <Button fluid={false} type="submit">
                Submit
            </Button>
        </Form>
    </Story>
</Preview>

### Fluid

Form fields and buttons can take the witdh of it's container.

<Preview>
    <Story name="fluid">
        <Form fluid>
            <Field>
                <Label>Where to?</Label>
                <TextInput />
            </Field>
            <Button>Submit</Button>
        </Form>
    </Story>
</Preview>

### Disabled

Form fields and buttons can be disabled.

<Preview>
    <Story name="disabled">
        <Form disabled>
            <Field>
                <Label>Where to?</Label>
                <TextInput />
            </Field>
            <Button>Submit</Button>
        </Form>
    </Story>
</Preview>

### Size

Form fields and buttons can vary in size.

<Preview>
    <Story name="size">
        <Inline align="end" gap={10}>
            <Form size="sm">
                <Field>
                    <Label>Where to?</Label>
                    <TextInput />
                </Field>
                <Button>Submit</Button>
            </Form>
            <Form>
                <Field>
                    <Label>Where to?</Label>
                    <TextInput />
                </Field>
                <Button>Submit</Button>
            </Form>
            <Form size="lg">
                <Field>
                    <Label>Where to?</Label>
                    <TextInput />
                </Field>
                <Button>Submit</Button>
            </Form>
        </Inline>
    </Story>
</Preview>

### Button alignment

Form buttons can be aligned horizontally.

<Preview>
    <Story name="button alignment">
        <Inline gap={10}>
            <Form>
                <Field>
                    <Label>Where to?</Label>
                    <TextInput />
                </Field>
                <ButtonGroup align="center">
                    <TextLink as="button">Reset</TextLink>
                    <Button>Submit</Button>
                </ButtonGroup>
            </Form>
            <Form>
                <Field>
                    <Label>Where to?</Label>
                    <TextInput />
                </Field>
                <ButtonGroup align="end">
                    <Button>Submit</Button>
                </ButtonGroup>
            </Form>
        </Inline>
    </Story>
</Preview>

### Formik

The form state and validation could be managed by an external library like [Formik](https://formik.org/).

<FilePreview filePath="/form/stories/RegistrationForm">
    <RegistrationForm />
</FilePreview>

<Story
    name="ref"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <RefLogger>
        <Form>
            <Field>
                <TextInput />
            </Field>
        </Form>
    </RefLogger>
</Story>

