import { Inline } from "@react-components/layout";
import { InnerTextLink, TextLink } from "@react-components/link";
import { Meta, Preview, Props, Story } from "@storybook/addon-docs/blocks";
import { ReactComponentsLayout } from "@stories/components";
import { RefLogger, paramsBuilder } from "@stories/utils";
import { RouterLink } from "../RouterLink";
import { Text } from "@react-components/text";
import { WarningIcon } from "@react-components/icons";

<Meta
    title="Navigation|TextLink"
    parameters={paramsBuilder()
        .canvasLayout({ width: "80%" })
        .docsLayout(<ReactComponentsLayout sourcePath="/packages/react-components/src/link/src" />)
        .build()}
/>

# TextLink

Links allow a user to navigate between pages, scroll to a have page section, access files, or even to handle email addresses.

<dl>
    <div>
        <dt>usage</dt>
        <dd>import TextLink from @orbit-ui/react-components</dd>
    </div>
    <div>
        <dt>slots</dt>
        <dd>left-icon | text | icon</dd>
    </div>
    <div>
        <dt>wai-aria</dt>
        <dd><a href="https://www.w3.org/TR/wai-aria-practices/#link">https://www.w3.org/TR/wai-aria-practices/#link</a></dd>
    </div>
</dl>

## Usage

### Icon

A text link can contain [icons](#).

<Preview>
    <Story name="icon">
        <TextLink href="#">
            <Text>Flight details</Text>
            <WarningIcon />
        </TextLink>
    </Story>
</Preview>

### Left icon

*Non standard* left [icons](#) can be provided to handle special cases. However, think twice before adding left icons, right icons should be your go to.

<Preview>
    <Story name="left icon">
        <TextLink href="#">
            <WarningIcon slot="left-icon" />
            <Text>Flight details</Text>
        </TextLink>
    </Story>
</Preview>

### External

A text link can indicate that his destination is an external resources. An external link will render with `target="_blank"` and `rel="noopener noreferrer"` attributes. These attributes values can be overrided by specifying a `target` or `rel` prop.

<Preview>
    <Story name="external">
        <TextLink external href="#">Flight details</TextLink>
    </Story>
</Preview>

### Button

A text link can be rendered as a button.

<Preview>
    <Story name="button">
        <TextLink as="button">Flight details</TextLink>
    </Story>
</Preview>

### React router

To integrate with a routing library like [React Router](https://reactrouter.com) a text link can be rendered as a [React Router Link](https://reactrouter.com/web/api/Link) or as any custom component.

<Preview>
    <Story name="react router link">
        <TextLink as={RouterLink} to="/flight-details">
            Flight details
        </TextLink>
    </Story>
</Preview>

### Color

A text link can use a different color accent.

<Preview>
    <Story name="color">
        <Inline>
            <TextLink color="primary" href="#">Primary</TextLink>
            <TextLink color="secondary" href="#">Secondary</TextLink>
            <TextLink color="danger" href="#">Danger</TextLink>
        </Inline>
    </Story>
</Preview>

### Disabled

A text link can look disabled.

<Preview>
    <Story name="disabled">
        <TextLink disabled href="#">Flight details</TextLink>
    </Story>
</Preview>

### Size

A text link can vary in size.

<Preview>
    <Story name="size">
        <Inline verticalAlign="end">
            <TextLink size="sm" href="#">Flight details</TextLink>
            <TextLink href="#">Flight details</TextLink>
        </Inline>
    </Story>
</Preview>

## Props

<dl>
    <div>
        <dt>slots</dt>
        <dd>left-icon | text | icon</dd>
    </div>
</dl>

<Props of={InnerTextLink} />

<Story
    name="ref"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <RefLogger>
        <TextLink href="#">Flight details</TextLink>
    </RefLogger>
</Story>
