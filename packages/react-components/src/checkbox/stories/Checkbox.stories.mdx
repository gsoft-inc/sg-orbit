import { Badge } from "@react-components/badge";
import { Checkbox, InnerCheckbox } from "@react-components/checkbox";
import { HelpIcon, InfoIcon } from "@react-components/icons";
import { HocWarning, InstallReactComponents, Props, ReactComponentsLayout } from "@blocks";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { RefLogger, createComponentSection, paramsBuilder } from "@utils";
import Imports from "./Imports.mdx";

<Meta
    title={createComponentSection("Checkbox")}
    parameters={paramsBuilder()
        .docsLayout(<ReactComponentsLayout semanticPath="/modules/checkbox" />)
        .build()}
/>

# Checkbox

A checkbox allows a user to select a value from a small set of options, often binary.

## Installation

<InstallReactComponents imports={<Imports />}/>

## Props

<HocWarning componentName="Checkbox" semanticPath="modules/checkbox/" />

<Props
    componentsDefinitions={[
        { displayName: "Checkbox", component: InnerCheckbox }
    ]}
/>

## Usage

### Text

A checkbox can have text.

<Preview>
    <Story name="text">
        <Checkbox text="Milky Way" />
    </Story>
</Preview>

### Checked

A checkbox can be checked.

<Preview>
    <Story name="checked">
        <Checkbox defaultChecked text="Milky Way" />
    </Story>
</Preview>

### Indeterminate

A checkbox can be indeterminate.

<Preview>
    <Story name="indeterminate">
        <Checkbox defaultIndeterminate text="Milky Way" />
    </Story>
</Preview>

### Disabled

A checkbox can be disabled.

<Preview>
    <Story name="disabled">
        <Checkbox disabled text="Milky Way" />
    </Story>
</Preview>

### Readonly

A checkbox can be readonly.

<Preview>
    <Story name="readonly">
        <Checkbox readOnly text="Milky Way" />
    </Story>
</Preview>

### No text

A checkbox doesn't require to have text.

<Preview>
    <Story name="no text">
        <Checkbox />
    </Story>
</Preview>

### Icons

A checkbox can display a single or multiple [icons](?path=/docs/materials-icons--page) after his text.

<Preview>
    <Story name="icons">
        <div className="flex items-lot-of-space-between">
            <Checkbox text="Milky Way" icons={<InfoIcon />} />
            <Checkbox text="Andromeda" icons={[<InfoIcon />, <HelpIcon />]} />
        </div>
    </Story>
</Preview>

### Badge

A checkbox can have a [badge](?path=/docs/components-badge--default-story) after his text.

<Preview>
    <Story name="badge">
        <div className="flex items-lot-of-space-between">
            <Checkbox text="Milky Way" badge={<Badge>6</Badge>} />
            <Checkbox text="Milky Way" badge={<Badge inline>6</Badge>} />
        </div>
    </Story>
</Preview>

### Size

A checkbox can vary in size.

Can be set to: `"small"`, `"large"` or omitted.

<Preview>
    <Story name="size">
        <div className="flex items-end items-lot-of-space-between">
            <Checkbox size="small" text="Milky Way" />
            <Checkbox text="Milky Way" />
            <Checkbox size="large" text="Milky Way" />
        </div>
    </Story>
</Preview>

<Story
    name="autofocus"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <div className="flex items-space-between">
        <Checkbox autofocus text="Milky Way" />
        <Checkbox autofocus disabled text="Andromeda" />
    </div>
</Story>

<Story
    name="autofocus delay"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <div className="flex items-space-between">
        <Checkbox autofocus autofocusDelay={500} text="Milky Way" />
        <Checkbox autofocus disabled autofocusDelay={500} text="Andromeda" />
    </div>
</Story>

<Story
    name="ref"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <RefLogger component={<Checkbox text="Milky Way" />} />
</Story>
