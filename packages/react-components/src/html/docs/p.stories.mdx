import { ComponentInfo, Preview, Tagline } from "@stories/components";
import { Meta, Story } from "@storybook/addon-docs";
import { P } from "@react-components/html";

<Meta
    title="Html elements/P"
    id="html-p"
/>

# P

<Tagline>A basic HTML paragraph element with styled component props.</Tagline>

<ComponentInfo
    usage={"import { P } from \"@orbit-ui/react-components\";"}
    githubPath="/packages/react-components/src/html/src"
/>

## Usage

### Example

A paragraph component accepts all the [HTML element attributes](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/p) and Orbit styled system props.

<Preview>
    <Story name="example">
        <P color="cloud-8">Apple co-founder Steve Wozniak tweeted on Sunday about a "private space company" he's launching that's "unlike the others." Called Privateer Space, its mission is to "keep space safe and accessible to all humankind".</P>
    </Story>
</Preview>
