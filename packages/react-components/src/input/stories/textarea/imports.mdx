import { ComponentImport } from "@blocks";

<ComponentImport
    bundle={`
        import { TextArea } from "@orbit-ui/react-components";
    `}
    standalone={`
        import { TextArea } from "@orbit-ui/react-input";
    `}
/>
