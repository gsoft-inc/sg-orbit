import { ArgsTable, Meta, Story } from "@storybook/addon-docs/blocks";
import { ComponentInfo, Preview } from "@stories/components";
import { Dot, InnerDot } from "@react-components/dot";
import { Inline } from "@react-components/layout";

<Meta
    title="Components/Dot"
    id="dot"
/>

# Dot

<ComponentInfo
    usage={"import { Dot } from \"@orbit-ui/react-components\";"}
    githubPath="/packages/react-components/src/dot/src"
/>

## Usage

### Default

A default dot.

<Preview>
    <Story name="default">
        <Dot />
    </Story>
</Preview>

### Label

A dot can have a label.

<Preview>
    <Story name="label">
        <Dot>Habitable</Dot>
    </Story>
</Preview>

### Color

A dot can have a color.

You can use a color name from our foundations (e.g. `sunray-50`, `primary-500`), an hexadecimal color code (e.g. `#c5cac8`), an `hsl`, an `hsla`, a `rgb` or a `rgba` color code.

<Preview>
    <Story name="color">
        <Inline>
            <Dot color="botanic-500">Habitable</Dot>
            <Dot color="hsl(25, 69%, 41%)">Uninhabitable</Dot>
            <Dot color="primary-500">Clean</Dot>
        </Inline>
    </Story>
</Preview>

## API

### Dot

<ComponentInfo
    usage={"import { Dot } from \"@orbit-ui/react-components\";"}
    compact
/>

<ArgsTable of={InnerDot} sort="alpha" />
