import { ComponentImport } from "@blocks";

<ComponentImport
    bundle={`
        import {
            RemoteSearchInput,
            searchInputResult,
            useDefaultResultsFetcher
        } from "@orbit-ui/react-components";
    `}
    standalone={`
        import {
            RemoteSearchInput,
            searchInputResult,
            useDefaultResultsFetcher
        } from "@orbit-ui/react-search-input";
    `}
/>
