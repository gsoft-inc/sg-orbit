import { ComponentImport } from "@blocks";

<ComponentImport
    bundle={`
        import {
            SearchInput,
            searchInputResult
        } from "@orbit-ui/react-components";
    `}
    standalone={`
        import {
            SearchInput,
            searchInputResult
        } from "@orbit-ui/react-search-input";
    `}
/>
