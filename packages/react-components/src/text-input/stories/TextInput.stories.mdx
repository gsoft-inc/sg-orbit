import { CheckCircleIcon, CrossIcon, MagnifierIcon } from "@react-components/icons";
import { ControlledTextInput } from "./ControlledTextInput.sample";
import { FilePreview, InstallReactComponents, Props, ReactComponentsLayout } from "@blocks";
import { IconButton } from "@react-components/button";
import { Inline } from "@react-components/layout";
import { InnerTextInput, PasswordInput, TextInput } from "@react-components/text-input";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { RefLogger, createComponentSection, paramsBuilder } from "@utils";
import Imports from "./Imports.mdx";

<Meta
    title={createComponentSection("TextInput")}
    parameters={paramsBuilder()
        .canvasLayout({ width: "80%" })
        .docsLayout(<ReactComponentsLayout sourcePath="/packages/react-components/src/text-input/src" />)
        .build()}
/>

# TextInput

A text input allow a user to enter and edit a text.

## Installation

<InstallReactComponents imports={<Imports />} />

## Props

<Props
    componentsDefinitions={[
        { displayName: "TextInput", component: InnerTextInput },
        { displayName: "PasswordInput", component: PasswordInput }
    ]}
/>

## Usage

### Default

A default text input.

<Preview>
    <Story name="default">
        <TextInput />
    </Story>
</Preview>

### Value

A text input can have a value.

<Preview>
    <Story name="value">
        <TextInput defaultValue="Deep space" />
    </Story>
</Preview>

### Prefix & Suffix

A text input can contain [icons](?path=/docs/materials-icons--page), [button](?path=/docs/components-button--default-story) or text elements.

<Preview>
    <Story name="prefix & suffix">
        <Inline>
            <TextInput prefix={<MagnifierIcon />} suffix={<IconButton><CrossIcon /></IconButton>} placeholder="Search..." />
            <TextInput prefix={<Text>$</Text>} suffix={<CheckCircleIcon />} placeholder="Amount" />
        </Inline>
    </Story>
</Preview>

### Size

A text input can vary in size.

Can be set to: `"small"`, `"large"` or omitted.

<Preview>
    <Story name="size">
        <Inline align="end">
            <TextInput size="small" fluid placeholder="Search..." />
            <TextInput fluid placeholder="Search..." />
            <TextInput size="large" fluid placeholder="Search..." />
        </Inline>
    </Story>
</Preview>

### Disabled

A text input can be disabled.

<Preview>
    <Story name="disabled">
        <TextInput disabled placeholder="Search..." />
    </Story>
</Preview>

### Readonly

A text input can be readonly.

<Preview>
    <Story name="readonly">
        <TextInput readOnly value="Deep space" />
    </Story>
</Preview>

### Loading

A text input can show that it is currently loading data.

<Preview>
    <Story name="loading">
        <TextInput loading placeholder="Search..." />
    </Story>
</Preview>

### Fluid

A text input can take the width of its container.

<Preview>
    <Story name="fluid">
        <TextInput fluid placeholder="Search..." />
    </Story>
</Preview>

### Color

A text input can use a different color accent.

<Preview>
    <Story name="error">
        <TextInput color="error" placeholder="Search..." />
    </Story>
</Preview>

### Transparent

A text input can be transparent.

<Preview>
    <Story name="transparent">
        <TextInput variant="transparent" placeholder="Search..." />
    </Story>
</Preview>

### Controlled

A text input can handle `value` in controlled mode.

<FilePreview filePath="/text-input/stories/ControlledTextInput">
    <ControlledTextInput />
</FilePreview>

<Story
    name="ref"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <RefLogger><TextInput /></RefLogger>
</Story>

## PasswordInput

A specialized text input allowing to show / hide the password.

<Preview>
    <Story name="password">
        <PasswordInput />
    </Story>
</Preview>
