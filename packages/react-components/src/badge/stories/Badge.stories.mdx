import { Badge, FloatingBadge, InnerBadge, InnerFloatingBadge } from "@react-components/badge";
import { CheckCircleIcon } from "@react-components/icons";
import { InstallReactComponents, Props, ReactComponentsLayout } from "@blocks";
import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { RefLogger, createComponentSection, paramsBuilder } from "@utils";
import { Square } from "./Square.jsx";
import { Stack, VerticalStack } from "@react-components/stack";
import Imports from "./Imports.mdx";

<Meta
    title={createComponentSection("Badge")}
    parameters={paramsBuilder()
        .canvasLayout({ width: "80%" })
        .docsLayout(<ReactComponentsLayout sourcePath="/packages/react-components/src/badge" />)
        .build()}
/>

# Badge

Badges contain numeric values and indicate a running tally. A badge is normally placed after the text of the thing it quantify or embedded in another component such as a button or a checkbox.

## Installation

<InstallReactComponents imports={<Imports />}/>

## Props

<Props
    componentsDefinitions={[
        { displayName: "Badge", component: InnerBadge },
        { displayName: "FloatingBadge", component: InnerFloatingBadge }
    ]}
/>

## Usage

### Default

A default badge.

<Preview>
    <Story name="default">
        <Badge>100</Badge>
    </Story>
</Preview>

### Highlight

A badge can draw attention on his text.

<Preview>
    <Story name="highlight">
        <Badge highlight>100</Badge>
    </Story>
</Preview>

### Size

A badge can vary in size.

Can be set to: `"tiny"`, `"small"`, `"large"` or omitted.

<Preview>
    <Story name="size">
        <VerticalStack>
            <Stack align="end">
                <Badge size="tiny">5</Badge>
                <Badge size="small">5</Badge>
                <Badge>5</Badge>
                <Badge size="large">5</Badge>
            </Stack>
            <Stack align="end">
                <Badge size="tiny">99</Badge>
                <Badge size="small">99</Badge>
                <Badge>99</Badge>
                <Badge size="large">99</Badge>
            </Stack>
        </VerticalStack>
    </Story>
</Preview>

### Inline

A badge can be rendered as inlined.

<Preview>
    <Story name="inline">
        <span>Galaxies<Badge variant="inline">100 billion</Badge></span>
    </Story>
</Preview>

An inline badge, is not affected by the `size` property. The badge will inherit it's size from his parent font-size.

<Preview>
    <Story name="inline size">
        <VerticalStack>
            <span className="f5">Galaxies<Badge variant="inline">100+</Badge></span>
            <span className="f4">Galaxies<Badge variant="inline">100+</Badge></span>
        </VerticalStack>
    </Story>
</Preview>

### Dot

A badge can be rendered as a dot. A single digit is supported.

<Preview>
    <Story name="dot">
        <VerticalStack>
            <Stack align="end">
                <Badge variant="dot" size="tiny" />
                <Badge variant="dot" size="small" />
                <Badge variant="dot" />
                <Badge variant="dot" size="large" />
            </Stack>
            <Stack align="end">
                <Badge variant="dot" size="tiny">5</Badge>
                <Badge variant="dot" size="small">5</Badge>
                <Badge variant="dot">5</Badge>
                <Badge variant="dot" size="large">5</Badge>
            </Stack>
        </VerticalStack>
    </Story>
</Preview>

### Icon

A badge can be an icon.

<Preview>
    <Story name="icon">
        <Stack align="end">
            <Badge variant="icon" size="tiny"><CheckCircleIcon /></Badge>
            <Badge variant="icon" size="small"><CheckCircleIcon /></Badge>
            <Badge variant="icon"><CheckCircleIcon /></Badge>
            <Badge variant="icon" size="large"><CheckCircleIcon /></Badge>
        </Stack>
    </Story>
</Preview>

### Floating

A badge can float to the top right corner of an element.

<Preview>
    <Story name="floating">
        <Stack>
            <FloatingBadge badge={<Badge variant="dot" />}>
                <Square />
            </FloatingBadge>
            <FloatingBadge badge={<Badge variant="dot" size="tiny">5</Badge>}>
                <Square />
            </FloatingBadge>
            <FloatingBadge badge={<Badge variant="icon"><CheckCircleIcon /></Badge>}>
                <Square />
            </FloatingBadge>
            <FloatingBadge badge={<Badge>9</Badge>}>
                <Square />
            </FloatingBadge>
            <FloatingBadge badge={<Badge>100</Badge>}>
                <Square />
            </FloatingBadge>
        </Stack>
    </Story>
</Preview>

<Story
    name="ref"
    parameters={paramsBuilder()
        .excludeFromDocs()
        .build()}
>
    <RefLogger>
        <Badge>100</Badge>
    </RefLogger>
</Story>


