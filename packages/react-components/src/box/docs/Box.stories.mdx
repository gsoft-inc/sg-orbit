import { ArgsTable, Meta, Story } from "@storybook/addon-docs/blocks";
import { ComponentInfo, Tagline } from "@stories/components";
import { InnerBox } from "@react-components/box";
import { paramsBuilder } from "@stories/utils";

<Meta
    title="Components/Box"
    id="box"
/>

# Box

<Tagline>A box is a general purpose component that can be used to compose new components or render a component into a [slot](?path=/docs/slots--page).</Tagline>

<ComponentInfo
    usage={"import { Box } from \"@orbit-ui/react-components\""}
    githubPath="/packages/react-components/src/box/src"
/>

## API

<ArgsTable of={InnerBox} />

<Story
    name="playground"
    args={{
        children: "Box"
    }}
    parameters={paramsBuilder()
        .component(InnerBox)
        .excludeFromDocs()
        .build()}
>
    {args => <InnerBox {...args} />}
</Story>
