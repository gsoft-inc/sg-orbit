{
    "name": "@orbit-ui/icons",
    "version": "0.2.0",
    "description": "Sharegate Orbit Icons",
    "repository": "https://github.com/gsoft-inc/sg-brand",
    "author": "Groupe Sharegate inc.",
    "license": "Apache-2.0",
    "main": "dist/index.js",
    "publishConfig": {
        "access": "public",
        "directory": "dist"
    },
    "scripts": {
        "start": "yarn watch",
        "prebuild": "yarn clean",
        "build": "babel src -d dist --copy-files",
        "postbuild": "yarn copy-to-dist",
        "prewatch": "yarn clean",
        "watch": "yarn build --watch",
        "clean": "rimraf dist",
        "prepublishOnly": "cross-env NODE_ENV=production && run-s build copy-license-to-dist",
        "copy-to-dist": "node scripts/copy-to-dist",
        "copy-license-to-dist": "node ../../scripts/copy-root-license --dest dist"
    },
    "peerDependencies": {
        "react": "*",
        "react-dom": "*"
    },
    "dependencies": {
        "@babel/runtime-corejs3": "*"
    }
}
