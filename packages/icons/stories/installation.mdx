import { Fragment } from "react";
import { InstallDependencies } from "@blocks";

<details><summary >install the packages</summary>

For icons as React components, install from the [React bundle](?path=/docs/getting-started-installation--page):

<InstallDependencies packageName="bundles/react" />

For static files:

```bash dark
npm install @orbit-ui/icons
```

</details>

<details><summary >then import what you need</summary>

As a multi-variant React component:

```jsx dark
import { IconName } from "@orbit-ui/react-components";
```

As a single variant React component:

```jsx dark
import { IconName24 } from "@orbit-ui/react-components";
```

As an image source:

```jsx dark
import IconName24 from "@orbit-ui/icons/icon-name-24.svg";
```

As a CSS background:

```css dark
.name-icon {
    background: url("~@orbit-ui/icons/icon-name-24.svg");
}
```

</details>
