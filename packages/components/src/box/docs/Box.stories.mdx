import { ArgsTable, Meta, Story } from "@storybook/addon-docs";
import { Box, InnerBox } from "@components/box";
import { ComponentInfo, Preview, Tagline } from "@stories/components";

<Meta
    title="Components/Box"
    id="box"
/>

# Box

<Tagline>A box is a general purpose component that can be used to compose new components, use the slot system or render styled components props.</Tagline>

<ComponentInfo
    usage={"import { Box } from \"@orbit-ui/components\";"}
    githubPath="/packages/components/src/box/src"
/>

## Usage

> Instead of using a box component, you should use one of Orbit's HTML element components, like a [Div](?path=/docs/html-div--example) component.

### Default

A default box.

<Preview>
    <Story name="default">
        <Box>Enjoy space like never before.</Box>
    </Story>
</Preview>

## API

<ComponentInfo
    usage={"import { Box } from \"@orbit-ui/components\";"}
    compact
/>

<ArgsTable of={InnerBox} sort="alpha" />
