{
    "npmClient": "yarn",
    "useWorkspaces": true,
    "version": "independent",
    "command": {
        "version": {
            "ignoreChanges": ["**/*.md", "**/*.mdx", "**/*.json", "**/*.config.js", "**/*rc*", "**/stories/*", "**/tests/*", "**/dist/*"],
            "allowBranch": ["master"]
        }
    }
}
