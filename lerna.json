{
    "npmClient": "yarn",
    "useWorkspaces": true,
    "version": "independent",
    "command": {
        "publish": {
            "version": {
                "ignoreChanges": ["**/*.md", "**/*.json", "**/*.config.js", "**/*rc*"]
            }
        }
    }
}
