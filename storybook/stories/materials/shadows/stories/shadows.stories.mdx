import { MaterialsLayout } from "../../materials-layout";
import { Meta } from "@storybook/addon-docs/blocks";
import { Table } from "@blocks";
import { TachyonsFile } from "@blocks";
import { createMaterialSection, paramsBuilder } from "@utils";
import Installation from "../../installation.mdx";

<Meta
    title={`${createMaterialSection("Shadows")}`}
    parameters={paramsBuilder()
        .docsLayout(<MaterialsLayout sourcePath="/packages/tachyons/src" />)
        .build()}
/>

# Shadows

Shadows are used to make content appear elevated.

## Installation

<Installation />

## Utilities

All Tachyons shadows utilities have been replaced to match Orbit Design values.

<Table
    columns={[
        { title: "Class", headerStyle: { width: "200px" } },
        { title: "Variable", headerStyle: { width: "225px" } },
        { title: "", headerStyle: { width: "400px" } }
    ]}
    rows={[
        [{ value: "shadow-100", className: "code f7 o-90" }, { value: "--shadow-100", className: "code f7 o-90" }, <div className="shadow-100 h9 ma3"></div>],
        [{ value: "shadow-200", className: "code f7 o-90" }, { value: "--shadow-200", className: "code f7 o-90" }, <div className="shadow-200 h9 ma3"></div>]
    ]}
    rowClassName="bg-white"
/>

## Tachyons

<TachyonsFile relativeFilePath="/shadows.css" />
