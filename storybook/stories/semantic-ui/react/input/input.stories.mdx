import { Meta, Preview, Story } from "@storybook/addon-docs/blocks";
import { Input } from "semantic-ui-react";
import { SemanticLink } from "@docs-blocks";
import { createComponentSection } from "@utils";
import PackageInstallation from "../package-installation.mdx";

<Meta title={createComponentSection("Input")} />

<SemanticLink docPath="/elements/input" />

# Input

## Installation

<PackageInstallation />

<details><summary >then import what you need</summary>

```javascript
    import { Input } from "semantic-ui-react";
```
</details>

## Usage

### Default

A default input.

<Preview>
    <Story name="default">
        <Input />
    </Story>
</Preview>

### Value

An input can have an value.

<Preview>
    <Story name="value">
        <Input value="Orbit" />
    </Story>
</Preview>

### Focused

An input can show a user is currently interacting with it.

<Preview>
    <Story name="focused">
        <Input focus placeholder="Search..." />
    </Story>
</Preview>

### Fluid

An input can take the width of its container.

<Preview>
    <Story name="fluid">
        <Input fluid placeholder="Search..." />
    </Story>
</Preview>

### Icons

An input can be formatted with an icon.

<Preview>
    <Story name="icon">
        <div className="flex flex-column mw5">
            <div className="mb4">
                <Input icon="search" placeholder="Search..." />
            </div>
            <div>
                <Input iconPosition="left" icon="search" placeholder="Search..." />
            </div>
        </div>
    </Story>
</Preview>

### Loading

An input can show that it is currently loading data.

<Preview>
    <Story name="loading">
        <div className="flex flex-column mw5">
            <div className="mb4">
                <Input loading icon="search" placeholder="Search..." />
            </div>
            <div>
                <Input loading iconPosition="left" icon="search" placeholder="Search..." />
            </div>
        </div>
    </Story>
</Preview>

### Disabled

An input can be disabled.

<Preview>
    <Story name="disabled">
        <Input disabled placeholder="Search..." />
    </Story>
</Preview>

### Error

An input field can show that the data contains errors.

<Preview>
    <Story name="error">
        <Input error placeholder="Search..." />
    </Story>
</Preview>

### Success

An input field can show that the data has been validated.

<Preview>
    <Story name="success">
        <Input className="success" placeholder="Search..." />
    </Story>
</Preview>

### Transparent

An input can be transparent.

<Preview>
    <Story name="transparent">
        <Input className="transparent" placeholder="Search..." />
    </Story>
</Preview>

