import { IntroductionLayout } from "./introduction-layout";
import { Meta } from "@storybook/addon-docs/blocks";
import { createIntroductionSection, paramsBuilder } from "@utils";

<Meta
    title={`${createIntroductionSection("Supported Platforms")}`}
    parameters={paramsBuilder()
        .sortPriority(-1)
        .docsLayout(<IntroductionLayout sourcePath="/" />)
        .build()}
/>

# Supported Platforms

These are the platforms that are supported by Orbit.

| Edge | Chrome | Firefox | Safari | IE |
|------|--------|---------|--------|----|
| 17 + | > 76   | 69      | > 12.2 | -  |
