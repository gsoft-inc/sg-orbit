---
name: SearchInput
menu: Components
route: /search-input
---

import "../styles/main.css";

import { Playground } from "docz";
import { noop } from "lodash";
import { SearchInput, searchInputResult } from "@sharegate/react-components";

# Search input

## Default

<Playground>
    <SearchInput results={[searchInputResult("1", "Mars"), searchInputResult("2", "Uranus"), searchInputResult("3", "Neptune"), searchInputResult("4", "Jupiter"), searchInputResult("5", "Pluto")]} onValueChange={noop} />
</Playground>

## Default Value

<Playground>
    <SearchInput results={[searchInputResult("1", "Mars"), searchInputResult("2", "Uranus"), searchInputResult("3", "Neptune"), searchInputResult("4", "Jupiter"), searchInputResult("5", "Pluto")]} defaultValue={"Neptune"} onValueChange={noop} />
</Playground>

## Clear on select

<Playground>
    <SearchInput results={[searchInputResult("1", "Mars"), searchInputResult("2", "Uranus"), searchInputResult("3", "Neptune"), searchInputResult("4", "Jupiter"), searchInputResult("5", "Pluto")]} clearOnSelect onValueChange={noop} />
</Playground>

## Disabled

<Playground>
    <SearchInput results={[searchInputResult("1", "Mars"), searchInputResult("2", "Uranus"), searchInputResult("3", "Neptune"), searchInputResult("4", "Jupiter"), searchInputResult("5", "Pluto")]} disabled onValueChange={noop} />
</Playground>
