---
name: DateRangePicker
menu: Components
route: /components/date-range-picker
---

import "../../styles/main.css";

import { DEFAULT_DATES_PRESETS, DateRangePicker } from "@sharegate/react-components";
import { Playground } from "docz";
import { noop } from "lodash";
import moment from "moment";

# Date Picker

## Default

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <DateRangePicker onDatesChange={noop} className="z-999" />
    </div>
</Playground>

## Selected Dates

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <DateRangePicker startDate={moment()} endDate={moment().add(3, "days")} onDatesChange={noop} />
    </div>
</Playground>

## Presets

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <DateRangePicker presets={DEFAULT_DATES_PRESETS} onDatesChange={noop} />
    </div>
</Playground>

## Min date restriction

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <DateRangePicker minDate={moment().subtract(2, "weeks")} onDatesChange={noop} />
    </div>
</Playground>

## Max date restriction

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <DateRangePicker maxDate={moment().add(1, "weeks")} onDatesChange={noop} />
    </div>
</Playground>

## Min and Max date restriction

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <DateRangePicker minDate={moment().subtract(2, "weeks")} maxDate={moment().add(1, "weeks")} onDatesChange={noop} />
    </div>
</Playground>

## Allow single date selection

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <DateRangePicker allowSingleDateSelection onDatesChange={noop} />
    </div>
</Playground>

## Inlined

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <div className="flex">
            <div className="mr4" style={{ width: "50%" }}>
                <DateRangePicker onDatesChange={noop} />
            </div>
            <div style={{ width: "50%" }}>
                <DateRangePicker onDatesChange={noop} />
            </div>
        </div>
    </div>
</Playground>

## Disabled

<Playground>
    <div style={{ minHeight: "400px", zIndex: "9999", position: "relative" }}>
        <DateRangePicker disabled />
    </div>
</Playground>
