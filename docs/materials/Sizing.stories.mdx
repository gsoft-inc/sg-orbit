import { Meta } from "@storybook/addon-docs";
import { Table } from "@stories/components";
import { Div } from "@components/html";

<Meta
    title="Materials/Sizing"
    id="sizing"
/>

# Sizing

Our sizing scale is used for dimension CSS properties lile `width`, `height`, `min-width`, `min-height`, etc.. If you need to space items use the [spacing scale](?path=/docs/spacing--page) instead.

### Units

Our sizing values are expressed in `rem` units based on a `font-size` of `16px`. We choosed `rem` units over `em` units since they are relative to the font-size of the root element instead of the parent element which can cause unexpected results when cascading.

For a deep understanding of rem units, refer to the [values and units](https://developer.mozilla.org/en-US/docs/Learn/CSS/Building_blocks/Values_and_units) article on MDN.

## Values

Orbit Design's sizing scale uses an 8px scale. We chose a base-8 scale because 8 is a highly composable number (it can be divided and multiplied many times and result in whole numbers), yet allows spacing dense enough for ShareGate's products. A few half points(4px) have been added to cover specific use cases that requires more granular sizing.

_Since dimensions higher than 480px are mostly used for layout, these values are not included in our sizing scale and should be set by your application, while keeping them a multiple of 16px._

<Table
    columns={[
        { title: "Prop Value", headerStyle: { width: "100px" }, rowClassName: "code"},
        { title: "CSS Variable", headerStyle: { width: "200px" }, rowClassName: "code" },
        { title: "Value", headerStyle: { width: "250px" }, rowClassName: "code" },
        { title: "", headerStyle: { width: "300px" } }
    ]}
    rows={[
        ["1", "--o-ui-sz-1", "0.75rem (12px)", <Div width={1} height={5} backgroundColor="warning-3"></Div>],
        ["2", "--o-ui-sz-2", "1,00rem (16px)", <Div width={2} height={5} backgroundColor="warning-3"></Div>],
        ["3", "--o-ui-sz-3", "1.25rem (20px)", <Div width={3} height={5} backgroundColor="warning-3"></Div>],
        ["4", "--o-ui-sz-4", "1.50rem (24px)", <Div width={4} height={5} backgroundColor="warning-3"></Div>],
        ["5", "--o-ui-sz-5", "2rem (32px)", <Div width={5} height={5} backgroundColor="warning-3"></Div>],
        ["6", "--o-ui-sz-6", "2.5rem (40px)", <Div width={6} height={5} backgroundColor="warning-3"></Div>],
        ["7", "--o-ui-sz-7", "3rem (48px)", <Div width={7} height={5} backgroundColor="warning-3"></Div>],
        ["8", "--o-ui-sz-8", "3.5rem (56px)", <Div width={8} height={5} backgroundColor="warning-3"></Div>],
        ["9", "--o-ui-sz-9", "4rem (64px)", <Div width={9} height={5} backgroundColor="warning-3"></Div>],
        ["10", "--o-ui-sz-10", "4.5rem (72px)", <Div width={10} height={5} backgroundColor="warning-3"></Div>],
        ["11", "--o-ui-sz-11", "5rem (80px)", <Div width={11} height={5} backgroundColor="warning-3"></Div>],
        ["12", "--o-ui-sz-12", "6rem (96px)", <Div width={12} height={5} backgroundColor="warning-3"></Div>],
        ["13", "--o-ui-sz-13", "8rem (128px)", <Div width={13} height={5} backgroundColor="warning-3"></Div>],
        ["14", "--o-ui-sz-14", "12rem (192px)", <Div width={14} height={5} backgroundColor="warning-3"></Div>],
        ["15", "--o-ui-sz-15", "16rem (256px)", <Div width={15} height={5} backgroundColor="warning-3"></Div>],
        ["16", "--o-ui-sz-16", "20rem (320px)", <Div width={16} height={5} backgroundColor="warning-3"></Div>],
        ["17", "--o-ui-sz-17", "24rem (384px)", <Div width={17} height={5} backgroundColor="warning-3"></Div>],
        ["18", "--o-ui-sz-18", "30rem (480px)", <Div width={18} height={5} backgroundColor="warning-3"></Div>]
    ]}
/>
