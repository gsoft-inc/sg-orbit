import { FoundationFile } from "./FoundationFile";
import { Header } from "@stories/components";
import { Meta } from "@storybook/addon-docs/blocks";
import { paramsBuilder } from "@stories/utils";

<Meta
    title="Getting Started/Foundation"
    id="foundation"
    parameters={paramsBuilder()
        .sortPriority(-4)
        .build()}
/>

# Foundation

Orbit UI foundation contains the [CSS variables](https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties) that store the visual design attributes of Orbit Design. We use those variables across Orbit UI instead of hard-coded values in order to maintain a scalable and consistent visual system for UI development.

Once [installed](?path=/docs/getting-started-installation--page), the default values can be [customized to your needs](#override-variables). Please be aware that customization would no longuer benefinits from updates and could break.

<Header
    usage={{
        apricot: "@import \"~@orbit-ui/foundation/apricot.css\";",
        overcast: "@import \"~@orbit-ui/foundation/overcast.css\";",
        desktop: "@import \"~@orbit-ui/foundation/desktop.css\";"
    }}
    githubPath="/packages/foundation/src"
/>

## Variables

You will find below an exhaustive list of all the variables available in Orbit UI foundation.

### Colors

<FoundationFile relativeFilePath="/core/colors/brand.css" />
<FoundationFile relativeFilePath="/core/colors/ui.css" />
<FoundationFile relativeFilePath="/core/colors/primary.css" />
<FoundationFile relativeFilePath="/products/apricot/colors.css" />
<FoundationFile relativeFilePath="/products/overcast/colors.css" />
<FoundationFile relativeFilePath="/products/desktop/colors.css" />

### Spacing scale

<FoundationFile relativeFilePath="/core/spacing-scale.css" />

### Type scale

<FoundationFile relativeFilePath="/core/type-scale.css" />

### Shadows

<FoundationFile relativeFilePath="/core/shadows.css" />

## Use variables

A foundation CSS variable can be accessed in a CSS class:

```css dark
.button {
    color: var(----primary-50);
}
```

In a `style` attribute:

```html dark
<button style="color: var(--primary-50)" type="button">
    Click me!
</button>
```

In JavaScript (the computed variable value is available as a string):

```javascript dark
window.getComputedStyle(document.documentElement).getPropertyValue("--primary-50");
```

## Override variables

Foundation CSS variables can be overrided by simply redeclaring the variables in a `:root` CSS class:

```css dark
:root {
    --primary-50: #c63a0c;
    --apricot-50: #c63a0c;
    --overcast-50: #c63a0c;
    --desktop-50: #c63a0c;
}
```

If you need to use a dynamic value or to scope an update to a specific element you can override a variable with a `style` attribute:

```html dark
<button style="--primary-50: #c63a0c" type="button">
    Click me!
</button>
```

At last resort, you can override a variable in JavaScript. However, we strongly encourage using a CSS class or a `style` attribute.

```javascript dark
document.body.style.setProperty("--primary-50", "#c63a0c");
```
